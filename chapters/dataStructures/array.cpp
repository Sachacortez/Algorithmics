#include<bits/stdc++.h>

using namespace std

int main() {
    int array[10];     // All 0's 
    memset(array, 0, 10);
    for(int i = 0; i< 10; i++) {
        array[i] = 0;
    }
    array[i];     // read O(n) = 1
    array[0] = 10     // write O(n) = 1
    return 0;
}